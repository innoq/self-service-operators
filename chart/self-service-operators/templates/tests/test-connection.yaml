apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "self-service-operators.fullname" . }}-test-connection"
  labels:
    {{- include "self-service-operators.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "self-service-operators.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
